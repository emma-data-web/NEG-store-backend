services:

  api:
    build: .
    env_file:
      - dev.env
    ports:
      - "8000:8000"

    

  worker:
    build: .
    env_file:
      - dev.env
    command: celery -A celery_app worker --loglevel=info
    

  redis:
    image: redis:alpine